// write your codes here
function setup(){
    createCanvas(600, 600);
    background(220);
}

function draw(){
for (let i = 0; i < 5; i++){
    let shade = 
    fill(0, shade, 0);
    ellipse(i * 50 + 25,i * 50 + 25,50,50);
}
}